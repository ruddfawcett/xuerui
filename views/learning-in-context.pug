// Created by Rudd Fawcett '18.
doctype html5
html(lang='en')
  head
    title 學銳 | #{assignment.name}
    link(rel='stylesheet', href='/static/assets/styles/lic.css')
    meta(name='viewport' content='width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no')
  body
    div.wrapper
      .sidebar
        header
          a(href='/')
            h3 XUERUI #[span.zh 學銳]
        .container#intro
          .header
            h2 Instructions
          p Take as much time as you  need to read through the passage. Pay attention to place the new words (highlighted in gray) in context of the broader lesson text. #[br]#[br] Use the buttons below to toggle the vocabulary of the presented words.
          .progress
            ul#toggle-text
              li.selected(data-lang='en') English
              li(data-lang='py') Pinyin
              li(data-lang='zh') Chinese
          p Once you feel like you've got a good understanding about what is happening in this passage, press the button below to start reviewing the vocabulary.
          .progress
            ul
              li#start-reviewing Start Reviewing
        .container#intro2
          .header
            h2 Instructions
          p You have one round of practice quizzing to go through the words and passage before your progress is logged.
        .container#progress
          .header
            h2 This Round
          .progress
            h3.number
              span.word-counter 0
              | /
              span.round-total 0
            p Word
          .progress
            h3.number
              span.percentage 0
              | %
            p Proficiency
          .progress
            h3.number
              span.round-master 0
              | /12
            p Round
      main
        section.text
          .title
            h1
          .body
            p
        section.module
          .container
            .options


block scripts
  script(type='text/javascript', src='https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.22.1/moment.min.js')
  script(type='text/javascript', src='https://ajax.googleapis.com/ajax/libs/jquery/3.0.0/jquery.min.js')
  script(type='text/javascript', src='/static/js/lic.app.js')
  script(type='text/javascript').
    var words = JSON.parse('!{words}');
    var Learn = new LearnMode("#{assignment.name}", "#{assignment.text}", words, 'en');
    Learn.start();
